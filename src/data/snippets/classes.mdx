---
title: classes
intro: Normalize strings, booleans, null and undefined into a valid React className string
createdAt: September 4, 2021
---

## Implementation

Add multiple classes that can also be type `boolean`, `null` and `undefined`.

```tsx:src/util/classes.ts
export function classes(...arr: (string | boolean | null | undefined)[]): string {
  return arr.filter(Boolean).join(" ");
}
```

## Usage

```tsx {2}
<div
  className={classes("myClass", isTrue && "anotherClass")}
>
  Hello world!
</div>
```
